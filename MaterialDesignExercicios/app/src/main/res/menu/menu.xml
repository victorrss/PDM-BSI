<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group
            android:id="@+id/main_menu_group"
            android:checkableBehavior="single">
        <item
                android:id="@+id/action_activity_home"
                android:checked="false"
                android:icon="@android:drawable/ic_menu_gallery"
                android:title="Home" />
        <item
                android:id="@+id/action_fragmento_produto"
                android:checked="false"
                android:icon="@android:drawable/ic_menu_gallery"
                android:title="Produto" />
        <item
                android:id="@+id/action_fragmento_cliente"
                android:checked="false"
                android:icon="@android:drawable/ic_menu_camera"
                android:title="Cliente" />
        <item
                android:id="@+id/action_fragmento_usuario"
                android:checked="false"
                android:icon="@android:drawable/ic_menu_preferences"
                android:title="Usuário" />
<!--        <item-->
<!--                android:id="@+id/action_activity_sobre"-->
<!--                android:checked="false"-->
<!--                android:visible="true"-->
<!--                android:icon="@android:drawable/ic_menu_info_details"-->
<!--                android:title="Sobre" />-->
    </group>
</menu>